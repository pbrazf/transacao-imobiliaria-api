# Prefixo base da API (usado no main.py para montar as rotas)
API_PREFIX=/api/v1

# Token fixo de autenticação (verificação via Authorization: Bearer <token>)
AUTH_BEARER_TOKEN=changeme123

# URL de conexão com o Postgres (ajustar no passo 2 com Docker)
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/pipeimob
